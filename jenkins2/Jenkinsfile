pipeline {

agent any

environment {
  registry = "https://hub.docker.com/"
  docker-credentials = "docker-cred"  
}

stages {
 stage ('get branch') {
   steps: {
		echo working branch is $GIT_BRANCH
   }
 
 }
 
 

}


}
